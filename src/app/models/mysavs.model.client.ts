export class MySavs {
	
	_id: string;
	mySavs_id: string;
	favsList: string;
	network: string;
	history: string;
	directMessage: string;
	

	// constructor(_id, mySavs_id) {
	// this._id = this._id;
	// this.mySavs_id = this.mySavs_id;
	// this.favsList = this.favsList;
	// this.network = this.network;
	// this.history = this.history;
	// this.directMessage = directMessage;
	// }
}